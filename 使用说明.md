# Screenshot Translator 使用说明

这份说明面向一台从未安装过本项目的新 Linux 机器。项目运行在 **KDE Plasma + X11** 或 **KDE Plasma Wayland** 图形会话中：程序负责截图和框选，Umi-OCR 在本机识别文字，Google Cloud Translation Basic v2 负责翻译，最后在原位置显示贴图。

项目同时支持两种会话；Wayland 会话使用 Spectacle 截图和 KGlobalAccel 全局快捷键，核心流程已在 Arch Linux + KDE Plasma Wayland 验证。新机器先确认当前会话：

```bash
echo "$XDG_SESSION_TYPE"
echo "$XDG_CURRENT_DESKTOP"
echo "$DISPLAY"
```

- X11 会话：`XDG_SESSION_TYPE` 为 `x11`，`DISPLAY` 有类似 `:0` 的值。
- Wayland 会话：`XDG_SESSION_TYPE` 为 `wayland`，`XDG_CURRENT_DESKTOP` 为 `KDE`。需要系统已安装 `spectacle`，以及 `gdbus`（glib2）或 `dbus-send`（dbus）之一。

## 1. 安装系统依赖

以 Debian、Ubuntu 或其衍生版为例：

```bash
sudo apt update
sudo apt install git python3 python3-venv python3-dev xz-utils libnotify-bin
```

其中 `python3-venv` 用来创建项目虚拟环境，`python3-dev` 用来处理部分 Linux 输入监听依赖的编译。项目本身不要求安装 Tesseract。

Wayland 会话额外需要（Arch 上通常在 Plasma 安装中已存在）：

```bash
# Debian/Ubuntu
sudo apt install spectacle glib2.0-bin
# Arch
sudo pacman -S spectacle glib2
```

`gdbus` 来自 glib2，`dbus-send` 来自 dbus；两者至少有其一即可注册 Wayland 全局快捷键。

如果 Umi-OCR 启动时提示缺少 Qt/XCB 动态库，再按报错补充系统库；常见的一组是：

```bash
sudo apt install libglib2.0-0 libgssapi-krb5-2 libgl1 libfontconfig1 \
  libfreetype6 libxcb-icccm4 libxcb-image0 libxcb-keysyms1 \
  libxcb-render-util0 libxcb-render0 libxcb-shape0 libxcb-xkb1 \
  libxcb-xinerama0 libxkbcommon-x11-0 libxkbcommon0 libdbus-1-3 \
  ttf-wqy-microhei
```

## 2. 克隆并安装项目

将 `<仓库地址>` 换成实际 Git 仓库地址。路径可以换成其他目录，下面使用 `$HOME/tools` 只是示例，不包含任何机器或用户信息。

```bash
mkdir -p "$HOME/tools"
git clone <仓库地址> "$HOME/tools/screenshot-translator"
cd "$HOME/tools/screenshot-translator"
./install.sh
```

`install.sh` 会在项目目录创建 `.venv`，并安装 `PySide6` 和 `pynput`。依赖不会安装到系统 Python 中。

如果安装时报 `ensurepip is not available`，执行：

```bash
sudo apt install python3-venv
```

如果系统同时安装了多个 Python 版本，再根据报错安装对应版本的 venv 包，例如 `python3.13-venv`。

## 3. 安装 Umi-OCR

X11 会话不需要另外安装 Spectacle、Flameshot 等截图软件，截图层已经由本项目提供。KDE Wayland 会话必须安装 `spectacle`：本程序先调用 Spectacle 全屏抓取，再显示自己的选择层，框选区域与 OCR 输入保持一致。必须额外安装的是 **Umi-OCR**，它负责本地 OCR，并通过 `127.0.0.1:1224` 提供 HTTP 接口。

从 [Umi-OCR 官方 Releases](https://github.com/hiroi-sora/Umi-OCR/releases/latest) 下载 Linux x64 的 Paddle 压缩包。当前项目验证过的目录结构如下；版本升级时把目录名和配置中的路径一起替换：

```text
$HOME/tools/Umi-OCR_Linux_Paddle_2.1.5/umi-ocr.sh
```

解压后确认启动脚本可执行：

```bash
chmod +x "$HOME/tools/Umi-OCR_Linux_Paddle_2.1.5/umi-ocr.sh"
```

项目默认会在需要 OCR 时自动执行这个脚本。也可以先手动启动一次观察错误：

```bash
"$HOME/tools/Umi-OCR_Linux_Paddle_2.1.5/umi-ocr.sh"
```

首次验证完成后按 `Ctrl+C` 退出即可；正式运行项目时，`ocr.auto_start = true` 会按配置自动启动 Umi-OCR。若换了安装目录，编辑配置文件中的 `ocr.umi_ocr`：

```toml
[ocr]
umi_ocr = "~/tools/Umi-OCR_Linux_Paddle_2.1.5/umi-ocr.sh"
```

Linux 版 Umi-OCR 需要 CPU 支持 AVX。可以用下面的命令检查：

```bash
lscpu | grep -i avx
```

没有输出时，先不要继续排查本项目，应更换支持 AVX 的机器或选择兼容的 OCR 方案。

## 4. 首次检查本地链路

安装脚本会创建：

```text
$HOME/.config/screenshot-translator/config.toml
```

先把翻译后端设为 `identity`，只验证截图、OCR 和贴图，不访问任何翻译服务：

```toml
[translation]
backend = "identity"
source = "auto"
target = "zh-CN"
```

然后运行检查：

```bash
cd "$HOME/tools/screenshot-translator"
./run.sh --check
```

关键项目按会话显示：

- X11：`会话: OK (x11 ...)`、`截图后端: OK (qt_x11)`、`全局快捷键(pynput): OK`、`Qt 屏幕: OK`。
- KDE Wayland：`会话: OK (wayland KDE)`、`截图后端: OK (spectacle: ...)`、`全局快捷键(KGlobalAccel): OK`、`Qt 屏幕: OK`。

Umi-OCR 没有运行但启动脚本存在时，检查可能显示警告；正式截图时程序会尝试自动启动它。

保持终端可见，运行：

```bash
./run.sh
```

按 `Ctrl+Alt+D`，框选一段文字。能完成 OCR 并显示原文贴图后，再配置 Google Cloud 翻译。默认快捷键和其他选项都在 `config.toml` 中。

只想识别文字时，用第二个快捷键 `Super+Ctrl+Shift+O`：不翻译，识别结果自动复制到剪贴板。它可以在 `config.toml` 的 `app.copy_hotkey` 中修改或留空禁用；只想改“只识别”模式的排版方式，改 `ocr.copy_parser`。

## 5. 注册 Google Cloud Translation API

本项目使用 **Cloud Translation - Basic v2 + API Key**，不需要 OAuth 登录流程，也不需要创建服务账号 JSON 文件。

1. 打开 [Google Cloud Console](https://console.cloud.google.com/)，创建或选择一个项目。
2. 在“结算/Billing”中为项目关联结算账号；如果账号有新用户试用额度，确认该项目使用的是预期的结算账号。
3. 打开“API 和服务 → 库”，搜索 **Cloud Translation API** 并点击“启用”。
4. 打开“API 和服务 → 凭据 → 创建凭据 → API 密钥”。
5. 编辑这个 API Key：在“API 限制”中选择“限制密钥”，只允许 **Cloud Translation API**。应用限制可按网络情况设置；没有固定出口 IP 的桌面网络不要随便填写一个 IP，否则会把自己的请求拦掉。
6. 只复制一次 Key 到本机配置文件，不要把 Key 写进项目、截图、日志、Issue 或聊天记录。

Google 官方说明：Basic v2 支持 API Key，Advanced v3 不支持 API Key；因此这里不要误选 Advanced v3。[认证说明](https://docs.cloud.google.com/translate/docs/authentication?hl=en)

## 6. 配置 API Key

项目提供不含真实凭据的模板。复制到用户配置目录并限制权限：

```bash
cd "$HOME/tools/screenshot-translator"
mkdir -p "$HOME/.config/screenshot-translator"
cp keys.example.toml "$HOME/.config/screenshot-translator/keys.toml"
chmod 600 "$HOME/.config/screenshot-translator/keys.toml"
```

编辑 `keys.toml`，只填写本机文件中的值：

```toml
[google_cloud]
api_key = "在这里填写你刚创建的 Key"
```

再编辑主配置：

```toml
[translation]
backend = "google_cloud"
source = "auto"
target = "zh-CN"
```

程序每次截图时读取 `$HOME/.config/screenshot-translator/keys.toml`。Key 不会写入日志；`keys.toml` 已被 `.gitignore` 忽略。不要执行会把整个文件打印到终端的命令，也不要把包含 Key 的终端截图上传到 Git。

## 7. 防止 API 用量失控

在 Google Cloud Console 的“配额”页面，只调整当前程序实际使用的 v2 通用翻译配额。建议先使用下面的保守值：

| 配额 | 建议值 |
| --- | ---: |
| `v2 and v3 general model characters per day` | `15000` |
| `v2 and v3 general model characters per minute` | `100000` |
| `v2 and v3 general model characters per minute per user` | `100000` |
| `Number of v2 default requests per minute` | `60` |
| `Number of v2 default requests per minute per user` | `60` |

保存方式是勾选对应行，点击铅笔图标或“编辑配额”，填入数值并提交。当前程序一次截图只发送一个批量翻译请求；`60` 次/分钟已经远高于单人桌面使用的正常频率。

可以把不使用的 AutoML、Translation LLM、romanization、transliteration、v3 文档翻译等配额设为 `0`。不要把上表中的 v2 通用字符或 v2 请求配额设为 `0`，否则程序会被配额直接拒绝。

`15000` 字符/天按 31 天计算约为 465,000 字符，适合先控制在每月 500,000 字符免费额度附近；具体价格和免费额度以[官方价格说明](https://cloud.google.com/products/translate/pricing)为准。Google 会把空格等字符计入用量；超出配额时会返回 403。配额按太平洋时间每日重置，修改后可能需要一段时间才生效。[官方配额说明](https://docs.cloud.google.com/translate/quotas?authuser=00)

再设置一个每月预算提醒，例如 `$20`，用于发现异常用量。预算提醒只是通知，不是硬停止；真正阻止继续请求的是每日字符配额。API Key 也要保留 API 限制，定期在凭据页面检查最近使用情况。Google 的 API Key 安全建议见[官方文档](https://docs.cloud.google.com/docs/authentication/api-keys-best-practices)。

## 8. 切换到正式运行

完成配额和 Key 配置后，重新执行：

```bash
cd "$HOME/tools/screenshot-translator"
./run.sh --check
./run.sh
```

按 `Ctrl+Alt+D` 截图测试。首次正式测试只选一小段文字，确认 Google Cloud 项目、API Key、配额和目标语言都正确后再正常使用。

## 9. 常见问题

### 从桌面图标启动后怎么退出

程序没有主窗口，从桌面快捷方式启动时也没有终端可 `Ctrl+C`。在项目目录执行：

```bash
./run.sh --quit
```

程序是单实例的，重复双击图标只会提示“已在运行”，不会启动第二个进程。托盘图标菜单里也可以直接选“退出”。

### Wayland 下按快捷键没有反应

先在“系统设置 → 快捷键”中确认存在 `Screenshot Translator` 的“截图并翻译”，并检查是否与其他全局快捷键冲突；也可以直接在配置中修改 `hotkey` 后重启程序。确认 `./run.sh --check` 中 `全局快捷键(KGlobalAccel)` 为 OK，并已安装 `gdbus`（glib2）或 `dbus-send`（dbus）。

### Wayland 下 Esc 或点击外部无法关闭贴图

Wayland 不允许应用进行全局键盘焦点抢占和全局鼠标监听，这是平台的限制：右键点击贴图、点贴图上的 `×`，或再次按 `Ctrl+Alt+D` 开始新截图（会先关闭旧贴图）。贴图支持左键按住拖动、滚轮缩放、中键点击归位。选择层无法用 `Esc` 取消时，点鼠标右键取消。

### Wayland 下截图失败

确认 `spectacle` 已安装且能在命令行运行：

```bash
spectacle --version
```

KDE Wayland 下本项目会执行 `spectacle -b -n -f -o <临时文件>`；失败信息会写入日志 `~/.local/state/screenshot-translator/app.log`。

### 检查显示没有图形会话

确认在 KDE Plasma 图形会话中运行，而不是在纯 TTY 或没有 `DISPLAY`/`WAYLAND_DISPLAY` 的 SSH 会话中。

### Umi-OCR HTTP 不可用

确认 `ocr.umi_ocr` 指向真实的 `umi-ocr.sh`，并检查本机 `127.0.0.1:1224` 是否被 Umi-OCR 使用。先直接运行启动脚本，看终端中的缺库或 CPU 报错。

### Google 返回 403

先检查 Cloud Translation API 是否已启用、项目是否关联正确的结算账号、Key 是否限制到了 Cloud Translation API，以及配额是否误设为 `0`。如果错误是 `Daily Limit Exceeded`，等待每日重置或提高每日字符上限；不要通过删除配额来绕过限制。

### Google 返回 400/401

检查 `keys.toml` 的 TOML 格式、文件权限和 Key 是否完整。不要把 Key 放进 `config.toml`、命令行参数或代码。

### 快捷键没有反应

确认应用运行在 X11，并检查 KDE 或其他程序是否占用了 `Ctrl+Alt+D`。可以先在 `config.toml` 中改成没有冲突的组合。

## 隐私和 Git 边界

- Umi-OCR 在本机运行；使用 `google_cloud` 时，OCR 识别出的文字会发送给 Google Cloud Translation API。敏感文字不要使用云翻译后端。
- 日志默认只记录运行元数据和错误，不记录截图像素、OCR 正文或译文正文。
- 真实 Key 只放在用户配置目录的 `keys.toml` 中，权限保持 `600`；不要提交、推送或粘贴它。
- 不要把含有个人路径、主机名、邮箱、Key、截图内容或日志正文的文件加入 Git。提交前检查 `git status` 和 `git diff --cached`。
- 本项目文档使用 `$HOME` 和占位符，不应写入具体用户名、主机名、邮箱或真实项目编号。
