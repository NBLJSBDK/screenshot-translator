# Screenshot Translator 协作说明

## 项目概览

这是一个面向 Linux + KDE Plasma + X11 的无主窗口截图翻译贴片工具（当前版本 `0.1.2`）。默认流程是：

```text
Ctrl+Alt+D → 框选区域 → Umi-OCR → 翻译 → 在原屏幕位置显示贴片
```

主要运行组件：

- `app.py`：PySide6 选择器、OCR/翻译流水线、贴图窗口、全局快捷键和点击监听。
- `config.example.toml`：XDG 配置模板。
- `install.sh`：创建项目 `.venv` 并安装 Python 依赖。
- `run.sh`：使用项目虚拟环境启动程序。
- Umi-OCR：默认位于 `~/tools/Umi-OCR_Linux_Paddle_2.1.5`，通过本地 HTTP API 提供 OCR。

当前 v0.1.x 只支持 X11，不要宣称支持 Wayland。程序不应增加普通主窗口、托盘界面或错误对话框，失败使用桌面通知和日志。

## 开始工作前

先阅读项目根目录的 `AGENT_HANDOFF.md`，再检查实际文件、运行环境和 Git 状态。不要根据交接文档臆测现场状态，也不要覆盖未知的用户修改。

仓库如果不可用，保留现状并报告原因；不要为了“修复”仓库而删除或重置未知内容。

## 安装与验证

项目依赖必须安装在 `.venv`，不要安装到系统 Python：

```bash
./install.sh
./run.sh --check
```

运行前应确认 `XDG_SESSION_TYPE=x11`、`DISPLAY` 已设置，并确认 Umi-OCR 可用。修改或排查翻译服务前，优先把配置中的 backend 设为 `identity`，先验证截图、OCR、渲染和贴图交互链路。

只有完成实际运行验证后，才能声称功能可用。日志默认不得写入截图内容、OCR 原文或翻译正文。

## 功能约束

- 默认快捷键为 `Ctrl+Alt+D`，且必须保持可配置。
- 贴图出现在所选屏幕区域；控件为紧凑的 `[原/译] [原] [译] [×]`。
- `Esc`、点击贴图和控件外部、`×` 可以关闭贴图；单纯失去焦点或 Alt+Tab 不得关闭。
- 贴图区域长按后可以拖动；再次触发截图时关闭旧贴图，只保留一个。
- 不要使用 `Qt.Popup` 实现贴图窗口。
- 不要把 Google 翻译成功当作本地截图/OCR/UI 验证的前提；该后端可能被限流。

## 提交消息格式

Git 提交消息使用约定式格式：

```text
<type>(<scope>): <简短说明>
```

其中 `scope` 可省略。`type` 只使用下列类型：

| type | 用途 | 示例 |
| --- | --- | --- |
| `feat` | 新功能 | `feat(auth): 增加微信登录功能` |
| `fix` | 修补 Bug | `fix(menu): 修复下拉菜单在移动端不显示的错误` |
| `refactor` | 重构，不改变功能意图 | `refactor: 简化逻辑判断函数` |
| `perf` | 性能优化 | `perf: 提高渲染效率` |
| `docs` | 文档变动 | `docs: 更新 API 使用说明` |
| `test` | 增加或调整测试 | `test: 添加登录模块单元测试` |
| `chore` | 构建过程或辅助工具变动 | `chore: 升级依赖库` |
| `wip` | 工作进行中 | `wip: 正在处理搜索建议逻辑` |

禁止使用以下提交类型：`add`、`update`、`modify`、`big`。

## 提交与推送确认

- 创建 Git 提交前，必须先向用户展示拟提交内容和提交消息，并获得明确确认。
- 执行 `git push` 前，必须先获得用户明确确认。
- 未获确认时，可以继续进行只读检查、编辑文件、运行测试和暂存变更，但不得提交或推送。
- 如操作需要额外权限或存在不确定性，应先向用户说明并申请。

## 工作要求

- 保留用户已有修改，不使用破坏性 Git 操作覆盖未知变更。
- 提交前检查工作树和暂存区，确保提交范围与说明一致。
- 提交消息使用中文简短说明，准确描述实际变更。
